import { Nunito } from 'next/font/google';

export const fontPrimary = Nunito({
    subsets: ['latin'],
    weight: "400",
    style: "normal",
});

export const fontPrimaryBold = Nunito({
    subsets: ['latin'],
    weight: "700",
    style: "normal",
});
